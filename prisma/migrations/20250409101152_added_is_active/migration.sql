BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[serviceOffering] ADD [IsActive] BIT NOT NULL CONSTRAINT [serviceOffering_IsActive_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
