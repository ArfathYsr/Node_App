BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Permission] ADD [StatusId] INT NOT NULL CONSTRAINT [Permission_StatusId_df] DEFAULT 1;

-- AddForeignKey
ALTER TABLE [dbo].[Permission] ADD CONSTRAINT [Permission_StatusId_fkey] FOREIGN KEY ([StatusId]) REFERENCES [dbo].[Status]([ID]) ON DELETE NO ACTION ON UPDATE NO ACTION;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
